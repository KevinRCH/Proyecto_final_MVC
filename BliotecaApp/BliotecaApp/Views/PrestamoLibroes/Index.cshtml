@model IEnumerable<BliotecaApp.Models.PrestamoLibro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Libro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prestamo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prestamo.Cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libro.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prestamo.PrestamoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prestamo.Cliente.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrestamoLibroId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrestamoLibroId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrestamoLibroId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
